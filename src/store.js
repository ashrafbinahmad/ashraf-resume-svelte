import { writable } from 'svelte/store';

export const scrollY = writable(0);
export const mainRoot = writable();
export const projectSection = writable();
export const showingAllCertificates = writable(false);
